[project]
name = "riftteam-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11",
]

[tool.setuptools]
packages = ["shared"]

[tool.setuptools.package-dir]
shared = "."
